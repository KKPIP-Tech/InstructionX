/* ═════════════════════════════════════════════════════════════════════════════
 * 滑块 (QSlider) - 修复截断问题版本
 * ═════════════════════════════════════════════════════════════════════════════ */

/* ─────────────────────────────────────────────────────────────────────────────
 * 水平滑块
 * ───────────────────────────────────────────────────────────────────────────── */

QSlider::groove:horizontal {
    border: none;
    height: 4px;
    background: {borderLight};
    border-radius: 2px;
}

QSlider::handle:horizontal {
    width: 14px;
    height: 14px;
    /* 负 margin 让 handle 超出 groove，需要容器有足够空间 */
    margin: -5px 0;
    background: {accent};
    border-radius: 7px;
    border: 2px solid white;
}

QSlider::handle:horizontal:hover {
    background: {accentLight};
}

QSlider::handle:horizontal:pressed {
    background: {accentDark};
}

QSlider::handle:horizontal:disabled {
    background: {borderLight};
    border: 2px solid {borderLight};
}

QSlider::sub-page:horizontal {
    background: {accent};
    border-radius: 2px;
}

QSlider::add-page:horizontal {
    background: {borderLight};
    border-radius: 2px;
}

/*
 * 【修复关键】使用更具体的选择器提高优先级
 * 容器高度 = handle 高度 (14px) + 上下 margin (5px × 2) + 安全边距 (2px) = 26px
 */
QSlider[orientation="horizontal"] {
    min-height: 24px !important;
    max-height: 24px !important;
}

/* 针对基础控件页面的滑块特别样式 */
QGroupBox > QGridLayout > QSlider[orientation="horizontal"] {
    min-height: 24px !important;
    max-height: 24px !important;
}

/* ─────────────────────────────────────────────────────────────────────────────
 * 垂直滑块
 * ───────────────────────────────────────────────────────────────────────────── */

QSlider::groove:vertical {
    border: none;
    width: 4px;
    background: {borderLight};
    border-radius: 2px;
}

QSlider::handle:vertical {
    width: 14px;
    height: 14px;
    /* 负 margin 让 handle 超出 groove，需要容器有足够空间 */
    margin: 0 -5px;
    background: {accent};
    border-radius: 7px;
    border: 2px solid white;
}

QSlider::handle:vertical:hover {
    background: {accentLight};
}

QSlider::handle:vertical:pressed {
    background: {accentDark};
}

QSlider::handle:vertical:disabled {
    background: {borderLight};
    border: 2px solid {borderLight};
}

QSlider::sub-page:vertical {
    background: {accent};
    border-radius: 2px;
}

QSlider::add-page:vertical {
    background: {borderLight};
    border-radius: 2px;
}

/*
 * 【修复关键】容器宽度 = handle 宽度 (14px) + 左右 margin (5px × 2) + 安全边距 (2px) = 26px
 */
QSlider[orientation="vertical"] {
    min-width: 24px !important;
    max-width: 24px !important;
    min-height: 100px !important;
}

/* 针对基础控件页面的垂直滑块特别样式 */
QGroupBox > QGridLayout > QSlider[orientation="vertical"] {
    min-width: 24px !important;
    max-width: 24px !important;
    min-height: 120px !important;
}

/* ─────────────────────────────────────────────────────────────────────────────
 * 旋钮 (QDial) - 移除无效的子控件选择器
 * ───────────────────────────────────────────────────────────────────────────── */

QDial {
    background-color: {controlFill};
    border: 1px solid {borderLight};
    border-radius: 50px;
    min-width: 48px;
    min-height: 48px;
}

QDial:hover {
    border: 1px solid {accent};
}

QDial:disabled {
    background-color: {borderLight};
    border: 1px solid {borderLight};
}

