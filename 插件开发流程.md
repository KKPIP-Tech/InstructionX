# InstructionX 插件开发快速指南

InstructionX 是一个功能强大的插件集成框架，通过组合不同功能的插件，创建个性化的一站式工具集。


## Part 1 初始化环境

InstructionX 框架基于 `Python 3.14.x` 版本以及 `PySide6` 开发，使用 `uv` 作为 Python 虚拟环境管理器。首先在计算机上安装 `uv` 管理器。

针对 IBM 兼容机（Windows 平台），在 PowerShell 使用下面的指令安装 `uv`:

```shell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

针对 Mac 机与 Linux 桌面平台使用下面的指令安装 `uv`:

```shell
curl -LsSf https://astral.sh/uv/install.sh | sh
```

之后在目标位置的文件夹中，进入 `PowerShell` 或 `终端(Terminal)`，从本项目 GitHub 仓库 clone 框架代码:

```shell
git clone https://github.com/KKPIP-Tech/InstructionX.git
```

进入目录

```shell
cd InstructionX
```

使用下面的指令，运行框架程序。第一次运行时，`uv` 管理器会根据 `uv.lock` 自动配置环境

```shell
uv run main.py
```

> 提示：`uv run` 会按 `uv.lock` 同步并创建虚拟环境后启动应用（框架推荐入口）。

## Part 2 配置插件项目仓库 

在正式开始前，请先确认框架开发分支：

- `main` 为 Release 分支，框架发版以及 `hotfix` 均在此分支；
- `dev` 为开发分支，框架的最新功能、Bug 修复均在此分支；
- `test` 为框架的测试分支；

之后请您先确认是官方开发者还是第三方开发者：

- 官方开发者：在框架目录下的 `plugin` 下初始化插件 git 仓库；
- 第三方开发者：在框架目录下的 `custom_plugin` 下初始化插件 git 仓库；

在插件开发的过程中，插件开发仓库应当存在 `main`、 `dev`、 `test` 至少三个分支，用于管理插件开发的代码，多人开发团队可以在此基础上按照需求进行扩充。

如果您有远程代码仓库，可以在初始化之后将三个分支推送至远程仓库。

## Part 3 AI 辅助

为更好适配 AI 开发流程，本仓库提供两种 AGENTS 文件，供 AI 使用。目前已知这两个文件对 `Kimi Code (CLI)`、`Claude Code (CLI)`、`OpenCode`、`Codex` 有良好的支持。

如您所见，在仓库根目录存在 `AGENTS.md` 与 `AGENTS-for-PLUGIN-DEV.md` 两个 AGENTS 参考文件。

如果您的工作在当前与 AI 的会话中着重涉及到框架开发内容：向 `InstructionX` 仓库提交 issues 或向仓库提交 Bug 修复的 `PR` 请求，则只需要直接向 AI 开发工具提需求即可。

如果您的工作在当前与 AI 的会话中着重涉及到插件开发，则使用下面的提示词作为与 AI 第一轮对话的内容，您需要根据需求对 `<>` 中的内容进行选择性编辑与保留： 

```
我是<官方/第三方>插件开发者，请你在 AGENTS.md 的基础上，以 `AGENTS-for-PLUGIN-DEV.md` 作为 AGENTS 参考文件。我当前正在<main/dev/test>分支开发插件。

我有一个需求，<在这里填入您的需求>，下面是详细的需求：
1. <需求细节1>
2. <需求细节2>
    ...
n. <需求细节n>

请你根据我的需求，进入计划模式，制定一个详细的、可行的实行计划。
```

## Part 4 框架文档

框架在 `docs/` 路径下提供了较为丰富和完善的开发文档。开发文档快速预览见 `docs/README.md`。

