# InstructionX 直接运行依赖
# 以 pyproject.toml 的 [project].dependencies 为单一来源,本文件与其保持同步,
# 供 uv pip install -r requirements.txt / pip install -r requirements.txt 直接安装使用。
PySide6>=6.10
requests>=2.32
aiohttp>=3.11
mcp>=1.28.1,<2
orjson>=3.11.0,<4
matplotlib>=3.10
# UIKit 图表引擎（alpha-v1.0.3 起）导入期依赖：大规模数据紧凑存储/降采样/坐标映射
numpy>=2.0
packaging>=23.0
qrcode[pil]>=7.4
# macOS Dock 图标设置（仅 macOS 安装，见 utils/macos_dock_icon.py）
pyobjc-framework-Cocoa>=11.0; sys_platform == "darwin"
