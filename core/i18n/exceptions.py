"""i18n 子系统异常定义。

正常路径（翻译缺失、文件损坏等）一律走回退链与日志，不抛异常；
本模块异常仅保留给编程错误（如非法的语言代码）场景。
"""


class I18nError(Exception):
    """i18n 子系统异常基类

    仅用于不可回退的编程错误；文案缺失等运行时问题不抛出本异常，
    由 FallbackResolver 按回退链处理（见 fallback.py）。
    """
